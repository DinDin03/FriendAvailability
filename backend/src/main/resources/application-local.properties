# Local Development Database Configuration - MySQL
spring.datasource.url=jdbc:mysql://localhost:3306/linkup_db
spring.datasource.username=linkup_user
spring.datasource.password=16October2003@
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA/Hibernate Configuration for Local Development
spring.jpa.database-platform=org.hibernate.dialect.MySQL8Dialect
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# Disable static content serving (we use React frontend)
spring.web.resources.add-mappings=false
spring.mvc.throw-exception-if-no-handler-found=true

# Local Development Settings
logging.level.com.linkups=DEBUG
logging.level.org.springframework.security=DEBUG

# Disable some production features for local development
spring.datasource.hikari.connection-timeout=30000
spring.datasource.hikari.maximum-pool-size=5

# Email Configuration for Local Development
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=${SMTP_USERNAME:himasha1977@gmail.com}
spring.mail.password=${SMTP_PASSWORD:ogqykedlvdjzhkea}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.starttls.required=true
spring.mail.properties.mail.smtp.ssl.trust=smtp.gmail.com
spring.mail.properties.mail.smtp.from=${SMTP_FROM_EMAIL:himasha1977@gmail.com}

# Local Email Verification Settings
app.email.verification.base-url=http://localhost:8080
app.email.from-name=LinkUp Team (Local Dev)
app.email.from-address=${SMTP_FROM_EMAIL:himasha1977@gmail.com}
BASE_URL=http://localhost:8080